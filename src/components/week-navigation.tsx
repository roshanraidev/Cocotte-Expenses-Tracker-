import Link from 'next/link';
import { ChevronLeft, ChevronRight, CalendarDays } from 'lucide-react';
import { addDays, weekLabel } from '@/lib/dates';
export function WeekNavigation({ monday, path, current }: { monday: string; path: string; current: string }) {
  return <section aria-label="Week selection" className="mb-6 rounded-xl border border-[#e4e6dc] bg-[#ffffff80] p-4"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-3"><CalendarDays size={18} className="text-[#af9658]"/><p className="text-sm font-semibold">{weekLabel(monday)}</p></div><nav className="flex items-center gap-2 text-xs" aria-label="Browse weeks"><Link className="icon-button" aria-label="Previous week" href={`${path}?week=${addDays(monday,-7)}`}><ChevronLeft size={17}/></Link><Link className="rounded-lg px-3 py-3 font-semibold" href={`${path}?week=${current}`}>This week</Link><Link className="icon-button" aria-label="Next week" href={`${path}?week=${addDays(monday,7)}`}><ChevronRight size={17}/></Link><details className="relative"><summary className="flex min-h-11 items-center rounded-lg border border-[#e4e6dc] bg-white px-3">Choose date</summary><form method="get" action={path} className="absolute right-0 z-20 mt-2 w-64 rounded-xl border border-[#e4e6dc] bg-white p-4 shadow-lg"><label htmlFor="week-picker">Choose any date in the week</label><input id="week-picker" type="date" name="week" defaultValue={monday} required/><button className="btn mt-3 w-full">View week</button></form></details></nav></div></section>;
}
