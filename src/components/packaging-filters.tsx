'use client';
import {useState} from 'react';
import type {Filters} from '@/lib/packaging/reporting';
export function PackagingFilterForm({filters,productId,suppliers,products}:{filters:Filters;productId?:string;suppliers:{id:string;name:string;active:boolean}[];products:{id:string;name:string;unit:string;packSize:string}[]}){
 const [period,setPeriod]=useState(filters.period);
 return <form className="card my-4 print:hidden"><div className="grid items-end gap-3 sm:grid-cols-2 lg:grid-cols-4"><label>Period<select name="period" value={period} onChange={e=>setPeriod(e.target.value)}><option value="week">Week · Monday–Sunday</option><option value="month">Month</option><option value="custom">Custom range</option><option value="undated">Undated invoices</option></select></label>{period==='week'&&<label>Week containing<input name="week" type="date" defaultValue={filters.start}/></label>}{period==='month'&&<label>Month<input name="month" type="month" defaultValue={filters.start.slice(0,7)}/></label>}{period==='custom'&&<><label>Start date<input name="start" type="date" defaultValue={filters.start}/></label><label>End date<input name="end" type="date" defaultValue={filters.end}/></label></>}<div className="mb-[7px]"><button className="btn">Apply filters</button></div></div><details className="mt-2" open={!!(filters.category||filters.supplier||(!productId&&filters.product))}><summary>Filter by category, supplier{!productId?' or product':''}</summary><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><label>Category<select name="category" defaultValue={filters.category}><option value="">All categories</option><option value="PACKAGING">Packaging</option><option value="CHEMICAL">Chemical</option><option value="UNCLASSIFIED">Unclassified</option></select></label><label>Supplier<select name="supplier" defaultValue={filters.supplier}><option value="">All suppliers</option>{suppliers.map(s=><option key={s.id} value={s.id}>{s.name}{!s.active?' (inactive)':''}</option>)}</select></label>{!productId&&<label>Product<select name="product" defaultValue={filters.product}><option value="">All products</option>{products.map(p=><option key={p.id} value={p.id}>{p.name} · {p.unit} {p.packSize}</option>)}</select></label>}</div></details><p className="mt-2 text-xs text-stone-500">By delivery date · GBP excluding VAT · Europe/London</p></form>;
}
